import React from "react";
import styles from "../../css/app/AddNote.module.scss";
import { PropTypes } from "prop-types";

const AddNote = () => {
  return (
    <div className={styles.AddNote}>
      {/*  */}
      {/*  */}
      {/*  */}
    </div>
  );
};

export default AddNote;

AddNote.defaultProps = {};

AddNote.propTypes = {};
